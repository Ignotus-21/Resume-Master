// The resume IR types now live in lib/resumeSchema.ts (typed mirror of
// backend/shared/resume.js). Re-exported here for existing imports.
export type { ResumeContent, ResumeDocument, DesignTokens, SectionKey, TemplateId, RenderMode, CompileError } from '@/lib/resumeSchema';
